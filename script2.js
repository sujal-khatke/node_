const data = require('./script');

console.log(data);
var arr = [2,3,1,9,12,3,5];
 // Filter function  jo hamesha array ke upper lagaya jata hai.

result =  arr.filter((item)=>{
   return item >=  4
 }
)
console.log(result);


// If you made a mistake in typing a command, you can use the Up Arrow key to cycle through your previous commands. This lets you find the one you want to correct, and then you can edit it before pressing Enter again.